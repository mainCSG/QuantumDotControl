general_info:

    # SAME NAMES AS INSTRUMENTS!
    voltage_device: sim900
    multimeter_device: agilent
    preamp_sensitivity: 1.0e-7 # A/V
    preamp_bias: 0.0 # V
    voltage_divider: 0.01 # Vout / Vin 
    voltage_resolution: 1.0e-3 # V